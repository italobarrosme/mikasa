# Hygraph (GraphCMS) GraphQL endpoint
NEXT_PUBLIC_API_URL=https://api-region.hygraph.com/v2/your-project-id/master

# Read-only content token. Exposed to the browser, so never use a token with
# write/mutation scope here.
NEXT_PUBLIC_API_TOKEN=your-read-only-token
